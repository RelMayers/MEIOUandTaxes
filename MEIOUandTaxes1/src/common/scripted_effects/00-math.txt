# Author: Phlopsi

# Example: sqrt_effect = { which = v_my_variable } 
# candidate for removal
sqrt_effect = {
	sqrt_variable = { which = $which$ }
}

# Inputs
#$which_x2$
#$which_y2$
#$which_x1$
#$which_y1$
#$which$
distance_2d_effect = { 
	set_variable = { which = Xdiff which = $which_x2$ }
	set_variable = { which = Ydiff which = $which_y2$ }

	subtract_variable = { which =  Xdiff which = $which_x1$ }
	subtract_variable = { which =  Ydiff which = $which_y1$ }

	multiply_variable = { which =  Xdiff which = Xdiff }
	multiply_variable = { which =  Ydiff which = Ydiff }

	change_variable = { which = Xdiff which = Ydiff} #no sum variable cause perfomance over clarity
	sqrt_variable = { which = Xdiff } #check log for errors 
	set_variable = { which = $which$ which = Xdiff } 

	set_variable = { which = Xdiff value = 0 }
	set_variable = { which = Ydiff value = 0 }
}
